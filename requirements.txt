# pip version must be >= 23.0 to support the new dependency resolver and avoid conflicts with older versions of TensorFlow. --- IGNORE ---
tensorflow>=2.13.0
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
seaborn>=0.12.0
matplotlib>=3.7.0
